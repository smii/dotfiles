# =========================================
# Hypridle Configuration (profile-aware)
# Profile-specific idle/lock behavior is loaded from the active profile.
# See: profiles/<name>/hypr/hypridle.conf
# =========================================
source = ~/.config/hypr/profile/hypridle.conf
