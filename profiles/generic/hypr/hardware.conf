# =========================================
# GENERIC PROFILE - Hardware-specific Hyprland config
# Minimal assumptions - works on most hardware
# =========================================

# Add system-specific hardware configuration here
# Examples:
#   env = LIBVA_DRIVER_NAME,nvidia     # For NVIDIA-only systems
#   env = GBM_BACKEND,nvidia-drm       # For NVIDIA-only systems
