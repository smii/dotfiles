# =========================================
# GENERIC PROFILE - Hardware-specific Hyprland config
# Minimal assumptions - works on most hardware
# =========================================

# Add system-specific hardware configuration here if needed.
# No GPU-specific envs set — uses default mesa/modesetting drivers.
# Examples (uncomment for NVIDIA-only systems):
#   env = LIBVA_DRIVER_NAME,nvidia
#   env = GBM_BACKEND,nvidia-drm
#   env = __GLX_VENDOR_LIBRARY_NAME,nvidia
