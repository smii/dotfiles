# =========================================
# GENERIC PROFILE - Any compatible system
# Hardware-specific packages
# =========================================

# --- Kernel (standard) ---
linux
linux-headers

# --- GPU ---
# No GPU-specific driver assumed; uses kernel's default (mesa/modesetting).
# Add nvidia-dkms, xf86-video-amdgpu, etc. manually if needed.
